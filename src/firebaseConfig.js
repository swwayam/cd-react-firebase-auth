import  {initializeApp} from "firebase/app";
import { GoogleAuthProvider, getAuth } from "firebase/auth";

const firebaseConfig = {
 // Your firebase config
};

// Initialize Firebase


// Initialize Auth 


// Initalize GoogleAuthProvider object.


export {auth , provider};